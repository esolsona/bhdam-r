# Core reference implementation and experiments (compare.py, experiments.py)
cryptography==46.0.6
zfec==1.6.0.0
numpy==2.4.4
matplotlib==3.10.8

# Optional: only needed for experiment_b_channels.py (real multi-channel transport)
# boto3               # Amazon S3
# azure-storage-blob  # Azure Blob Storage
# paramiko            # SFTP/SSH
